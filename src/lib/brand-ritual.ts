/**
 * Single sanctioned brand mechanic: icon + copy, repeated in exactly three
 * surfaces (hero, menu, concierge) — not a decorative pattern everywhere.
 */
export const BRAND_RITUAL = {
  shortLabel: "The Gold Line",
  /** One line guests remember — pass-to-table continuity (fictional brand). */
  hook: "One unbroken line from pass to table.",
} as const;
